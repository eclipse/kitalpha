<%
String outputFolder = ctx.getValue("outputFolder").toString();
String projectName = ctx.getValue("projectName").toString();
String text = ((EEnum) element).getName();
text = EscapeChars.forHTML(text);
%>
<p><strong>Package :</strong> <%=org.polarsys.kitalpha.doc.gen.business.ecore.helpers.EcoreModelHelper.getTypeHyperLink(((EEnum) element).getEPackage())%></p>
<h1><%=text%></h1>

<p><strong>Default value :</strong> <%=((EEnum) element).getDefaultValue()%></p>
<%
String literalList = org.polarsys.kitalpha.doc.gen.business.ecore.helpers.EEnumContentHelper.getLiteralsList((EEnum) element, projectName, outputFolder);
if(literalList != "") {
%>
<h2>Literals</h2>
<%=literalList%>
<%}%>